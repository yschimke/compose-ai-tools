{
  "_doc": [
    "The symbol surface between `ee.schimke.composeai.cli.serve` and the rest of the CLI.",
    "A debt register for the preview-server split (issue #3824), not a config file: entries",
    "only ever come off. `scripts/check-serve-seam.py` fails on a crossing that is not listed",
    "(new coupling) AND on a listed entry that no longer crosses (prune it \u2014 that is progress).",
    "Regenerate the shrunk list with `python3 scripts/check-serve-seam.py --write`.",
    "cliInternalsUsedByServe: serve reaching into the CLI. Each entry is something the extracted",
    "server would have to get from a published contract module instead. The bundle-format cluster",
    "(BundleReader, BundleSigning, BundleClasspathHydration, extractBundle*, locateBundleSidecar*,",
    "ZIP_DOS_EPOCH_MS, downscaleRaster) used to be most of this list and is now off it \u2014 #3824",
    "preparation item 5. Those symbols live in a published `:bundle-format` whose package,",
    "`ee.schimke.composeai.bundle`, is mapped under contractPackages below, so serve names a",
    "contract instead of a CLI internal. BUNDLE_VERSION stays: it is still declared in `:cli`.",
    "Note what made the difference. Moving the files into a module did not shrink this list at",
    "all, because the check keys a crossing on the package an import names, not on the module",
    "that declares it, and the extraction kept the `\u2026cli` package for source-compat. The rename",
    "is what moved the number.",
    "Applying that lesson a second time took this list from 9 to 3. Six of the nine that remained",
    "after item 5 were never CLI internals at all: PreviewInfo, PreviewManifest and PreviewParams",
    "belong to the published :preview-data-api, and PreviewModule, PreviewParameterFanout and",
    "PreviewResultBuilder to :gradle-preview-driver \u2014 all six only looked like coupling because",
    "they shared the `\u2026cli` package. Both modules now carry their own packages",
    "(`ee.schimke.composeai.previewdata` / `\u2026previewdriver`), which is a breaking change for",
    "external consumers and was made deliberately rather than papered over in this file.",
    "That rename exposed something the package sharing had hidden: serve used three types from",
    "`:gradle-preview-driver`, a module whose api dependency is the Gradle Tooling API. None of",
    "the three needed Gradle \u2014 PreviewParameterFanout has no imports at all, PreviewResultBuilder",
    "and previewSha256 use only Okio and the DTOs \u2014 so they moved down into :preview-data-api",
    "rather than dragging the Tooling API onto an extracted server's floor. `\u2026previewdriver` has",
    "no mapping below because serve no longer names it.",
    "The three left are genuinely declared in `:cli` and each needs a decision, not a move:",
    "BUNDLE_VERSION (does an extracted server report its own version?), CoordinateResolver, and",
    "WebEmbed. They are #3824 preparation item 7's problem.",
    "Those three are now resolved, and cliInternalsUsedByServe is EMPTY in both source sets \u2014 the",
    "serve->cli direction of this register is closed. BUNDLE_VERSION became serve's own",
    "SERVE_VERSION (a deployed server reports what the server is, not what the CLI that launched",
    "it is); CoordinateResolver moved into a published :bundle-coordinates, mapped below;",
    "WebEmbed moved into the serve package, which turns it from a serve->cli crossing into a",
    "cli->serve one \u2014 the direction that does not block extraction, since an extracted `:cli`",
    "depends on the server module rather than the other way round.",
    "What is left is serveInternalsUsedByCli, 102 on main, dominated by ServeCommand.kt. That is",
    "the rest of preparation item 7.",
    "serve is a Gradle module now (:cli:serve), so the serve->cli direction is enforced by the",
    "build rather than by this file: :cli depends on it, Gradle rejects the reverse, and",
    "checkServeModuleBoundary fails on the resolved runtime classpath if :cli, a renderer or the",
    "plugin arrives transitively. This scanner still owns the cli->serve direction, which the",
    "build permits and the split needs to shrink; 92 of the 102 are ServeCommand.kt alone.",
    "serveInternalsUsedByCli: the CLI reaching into serve. Each entry must either move to the",
    "server build with serve, or move down into a contract module the CLI can keep using. The",
    "long tail here is ServeCommand.kt, which #3824 wants reduced to a thin entry point.",
    "forbiddenPackages is NOT a ratchet: an extracted preview server never imports a renderer, an",
    "forbiddenPackages is NOT a ratchet: an extracted preview server never imports a renderer, an",
    "MCP server, or the gradle plugin, and there is nothing here to grandfather. `\u2026composeai.plugin`",
    "is the plugin's real namespace (`plugin`, `plugin.daemon`, `plugin.tooling`) \u2014 an earlier",
    "`\u2026composeai.gradle` entry named nothing at all and enforced nothing, while `plugin.tooling`",
    "types sit on the CLI compile classpath via `:gradle-preview-driver`, ready to be reached for.",
    "contractPackages maps each non-`cli` package serve imports to the module that publishes",
    "it. The contract probe that cross-checked this list went with the extraction, so this",
    "register is now the only thing watching the floor: a package serve imports that is not",
    "mapped here fails check-serve-seam.py, and the module it names must be PUBLISHED, since",
    "the server resolves by coordinate from a repository now.",
    "unpublishedContracts records a mapped module that cannot be resolved as an artifact yet;",
    "each one is a split blocker, not an exemption. It is empty today: every module serve",
    "imports from is published. `daemon.bta` maps to `daemon-core`, not to `:daemon:bta-host` \u2014",
    "the package is declared by BOTH modules and the types serve uses (BtaCompileSession,",
    "DiagnosticCollector) are the `daemon-core` ones. A package name is not a module name; the",
    "test asserts each mapping against the module that actually declares the imported type.",
    "reflectiveDependencies records a module serve reaches by class name in a string literal:",
    "subprocess main classes and classloader-loaded sidecars. No import scan can see one, and",
    "the contract probe cannot either, because it never stages them. This is the one category",
    "where these checks are blind by construction, so the register is manual, pinned by a test",
    "that each literal still exists, and guarded by a test that finds unrecorded ones. Two of",
    "the four are unpublished modules \u2014 the real split blockers, in place of the one this PR",
    "first reported wrongly."
  ],
  "forbiddenPackages": [
    "ee.schimke.composeai.renderer",
    "ee.schimke.composeai.mcp",
    "ee.schimke.composeai.plugin"
  ],
  "contractPackages": {
    "ee.schimke.composeai.agentgrants": "agent-grant-protocol",
    "ee.schimke.composeai.bundle": "bundle-format",
    "ee.schimke.composeai.bundle.coordinates": "bundle-coordinates",
    "ee.schimke.composeai.daemon": "daemon-core",
    "ee.schimke.composeai.daemon.bta": "daemon-bta",
    "ee.schimke.composeai.daemon.client": "daemon-client",
    "ee.schimke.composeai.daemon.devices": "daemon-devices",
    "ee.schimke.composeai.daemon.protocol": "daemon-protocol",
    "ee.schimke.composeai.data.layoutinspector": "data-layoutinspector-core",
    "ee.schimke.composeai.data.overrides": "data-preview-overrides-core",
    "ee.schimke.composeai.data.pseudolocale": "data-pseudolocale-core",
    "ee.schimke.composeai.data.remotecompose": "data-remotecompose-core",
    "ee.schimke.composeai.data.render": "data-render-core",
    "ee.schimke.composeai.data.theme": "data-theme-core",
    "ee.schimke.composeai.designpages": "preview-data-api",
    "ee.schimke.composeai.imagecrop": "common-image-crop",
    "ee.schimke.composeai.io": "common-io",
    "ee.schimke.composeai.previewdata": "preview-data-api",
    "ee.schimke.composeai.render.session": "render-session-api",
    "ee.schimke.composeai.render.session.subprocess": "render-session-subprocess",
    "ee.schimke.composeai.web": "common-web-escaping",
    "ee.schimke.composeai.xr": "preview-data-api"
  },
  "unpublishedContracts": {},
  "reflectiveDependencies": {
    "ee.schimke.composeai.usagepsi.UsageSourceAnalyzer": {
      "file": "cli/serve/src/main/kotlin/ee/schimke/composeai/cli/serve/UsageSourceFacts.kt",
      "module": "usage-source-psi",
      "published": false,
      "why": "The playground's source cleaner loads :usage-source-psi in an isolated classloader and calls one String->JSON method. It degrades: absent, the cleaner falls back to its text passes, so an extracted server without it loses parse-quality cleaning rather than breaking. Staged by cli/build.gradle.kts through the composePreviewUsagePsi configuration."
    },
    "ee.schimke.composeai.rcembedded.jvm.RcJvmRenderMainKt": {
      "file": "cli/serve/src/main/kotlin/ee/schimke/composeai/cli/serve/RcJvmServerRenderer.kt",
      "module": "third-party-rc-embedded-player-jvm",
      "published": false,
      "why": "Subprocess main class for the embedded Remote Compose player. #3824 already lists this module among the sidecars the CLI tarball stages; it is an in-repo project dependency today and would have to become a published artifact for the split."
    },
    "ee.schimke.composeai.rcembedded.jvm.RcJvmRenderWorkerMainKt": {
      "file": "cli/serve/src/main/kotlin/ee/schimke/composeai/cli/serve/RcJvmWorkerPool.kt",
      "module": "third-party-rc-embedded-player-jvm",
      "published": false,
      "why": "The worker-pool half of the same sidecar."
    },
    "ee.schimke.composeai.daemon.DaemonMain": {
      "file": "cli/serve/src/main/kotlin/ee/schimke/composeai/cli/serve/ServeBundleDaemon.kt",
      "module": "daemon-desktop / daemon-android",
      "published": true,
      "why": "Main class of the daemon JVM serve spawns for a bundle. A process-launch string, not a compile dependency \u2014 serve never links against a daemon implementation, and checkCliDaemonLibraryBoundary still forbids it on the classpath. Both modules are published, so this is staging rather than a blocker."
    }
  },
  "main": {
    "cliInternalsUsedByServe": [],
    "serveInternalsUsedByCli": [
      "serve.PreviewHistory",
      "serve.PreviewHistoryManifest",
      "serve.RenderFailureFrame",
      "serve.RenderOutcome",
      "serve.ServeBuildHost",
      "serve.ServeBundleDaemon",
      "serve.ServeCommandOptions",
      "serve.ServeDiscovery",
      "serve.ServeRenderHost",
      "serve.ServeRunner",
      "serve.SvgOutcome"
    ]
  },
  "test": {
    "cliInternalsUsedByServe": [],
    "serveInternalsUsedByCli": [
      "serve.FakeRenderSession",
      "serve.PreviewHistoryManifest",
      "serve.ServeAgentGrantStore",
      "serve.ServeBackgroundWork",
      "serve.ServeBundleHost",
      "serve.ServeHttpServer",
      "serve.ServeImageStore",
      "serve.ServeImageUploadAuth",
      "serve.ServeParameterRows",
      "serve.ServePreview",
      "serve.ServeRenderHost",
      "serve.ServeRunner",
      "serve.ServeSessionRegistry"
    ]
  }
}
