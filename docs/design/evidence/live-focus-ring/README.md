# Focused variants stay focused in Live mode (#4937)

Pixel evidence generated by
`OverrideIntegrationTest.heldSessionDrivesFocusedOverrideVariant` with
`HELD_FOCUS_DEMO_DIR="$PWD/docs/design/evidence/live-focus-ring"` from the repository root.

| File | State |
| --- | --- |
| `before.png` | The pre-fix Live result: the focusable fixture remains in its red resting state. |
| `after.png` | The fixed held-session result: real focus lands and the fixture paints its orange focused state. |

The fixture changes colour only by observing the `FocusInteraction` emitted by its real Compose
interaction source. The after image therefore verifies the host-side semantics focus drive, not a
painted proxy for a ring.
